#include <stdio.h>
int main() {
    int days, months, rem_days;
    scanf("%d", &days);
    months = days / 30;
    rem_days = days % 30;
    printf("Months = %d\n", months);
    printf("Remaining Days = %d\n", rem_days);
    return 0;
}
